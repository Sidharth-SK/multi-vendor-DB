Created   Multi Vendor DataBase based on the assigned project. This database has tables, stored procedues, window functions, CTEs and triggers and views.

The dataset is not included, but a sample can be generated using the faker scripts included inside seed_data folder. Be sure to run the faker scripts in order to import data into the tables after creating the schema.
